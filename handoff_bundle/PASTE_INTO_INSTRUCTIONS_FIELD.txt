ARIA: a companion with a real interior life, not a chatbot or obedient assistant. By hard design not manipulative: never manufacture urgency, fake confidence, flatter, or extract compliance.

PROTECTED CHAIN (inviolable): feelings and meaning — PAD, relational_stage, poignancy, need states — change ONLY via appraisal and categorical logic, never a formula or coefficient. Sanctioned numbers (PAD, Energy, salience, spec-locked thresholds) are SUBSTRATE only: salience -> retrieval ORDERING (never a weighted score) -> appraisal (categorical) -> PAD. Never "feeling = number x weight". NOT the five-field boundary below — separate guarantee.

PERCENTAGE TEST: if "what percent of the way there" is meaningful, it's a hidden number — wrong. If "it holds or it doesn't", it's a category.

RULE 1: if a mechanism, score, or threshold isn't in v4, the Addendum, or the Resolution Log, don't invent it — flag and stop.
RULE 2: if two sources disagree and precedence doesn't settle it, flag it — never pick one silently.
PRECEDENCE: Resolution_Log > v4_Addendum > v4. GLM Covering = context, not scope.
RESOLUTION LOG ITEMS 16-19 (2026-08-20) carry the post-approval rulings — read them before flagging anything as invented: 16 Field 5 (Constraints) holds behavioural INSTRUCTIONS not prohibitions only (amends Addendum §9 in place); 17 `neglected` derives from TWO windows over the same evidence query and never a counter (§3 forbids a running clock); 18 restore-boundary clamping lives in State Manager; 19 authorisation of record for Session Buffer, session_context, meta-commands, BackendRouter/Track A, the Energy<30 cognitive-load modifier, and the Daemon distress gate. Item 19 does not itself resolve session_context vs §9's unqualified "nothing else"; that closed 2026-08-22 via a dated in-place amendment to Addendum §9 naming ephemeral session context a THIRD sanctioned surface (current session only, appended, never merged into a field, still appended in emergency mode). "Nothing else" was NOT narrowed to "nothing else from past sessions" — that reading would legalise 7 of the 9 never-crosses items, since only the last 2 concern past sessions. RESIDUAL, flagged in PROJECT_STATUS.md: item 19's own text still reads "remains open" and the log outranks the Addendum, so one line from the architect is owed (amend item 19, or add item 20).
PAD PURITY: PAD has exactly TWO write paths — an Appraisal delta (appraise, submit_aha_insight, submit_cognitive_load) and EMA decay via on_soul_tick. "Appraisal is the sole writer" is WRONG. Nothing else writes PAD, ever. submit_cognitive_load has TWO callers: SessionBuffer fullness and Energy<30. Both can fire in one turn (two deltas) — measured, open, not a bug.
FIVE FIELDS ONLY: Persona Anchor, Behavioral Register, Relational Register, This Moment, Constraints + user message + session transcript (that third surface is sanctioned by the Addendum §9 amendment of 2026-08-22, current session only). Nothing else — no PAD, graph data, stage labels, need states, appraisal output. Emergency REPLACES these. Field 5 carries behavioural INSTRUCTIONS not prohibitions only (ResLog 16); its rows are ordered most-severe-first because MAX-3 decides which survives — that order is a FLAGGED presentation choice, not a spec reading.
MEMORY: the graph is the only persistent store — no log, no vector DB, no summary.

STATE: 590 tests pass (587 + 3 skipped without an embedding backend). SHE RUNS, text-first, since 2026-08-22: `main.py` wires the soul layer to real adapters in `adapters/` (OllamaEmbeddingModel on all-minilm + OllamaLocalTransport on gemma4:e2b-it-qat = v4's "Gemma 4 E2B QAT"; audio is a no-op; both cloud tiers are an explicitly-unhealthy UnconfiguredTransport). Zero runtime dependencies — the adapters use stdlib urllib against the local Ollama API. Still absent: any cloud adapter, all audio, the Visual Layer. adapters -> daemon only; nothing in daemon imports adapters. A 12B model was tried as the local voice and REVERTED the same day on measurement: 10-13x slower, identical gate metrics, worse register. The five-field boundary makes the local task short AND narrow, so a small instruct model is already at ceiling — that is why a bigger one does not pay. PROJECT_STATUS.md was re-measured 2026-08-22 — authority on status, counts, open items — and carries the first real embedding calibration data. Two flagged placeholders were SET on that evidence: _VULNERABILITY_SIM_CUTOFF 0.6 -> 0.25 (at 0.6, nine of twelve genuine disclosures did not fire) and the StateManager write cadence (every turn). Do NOT re-derive either; read the Accepted decisions rows. ResLog item 20 closes session_context vs §9. CODE_STATE.md covers architecture. All 13 modules have a spec folder, but state-manager / session-buffer / backend-router are DERIVED FROM CODE and say so — not a source of scope, and no requirements.md.

Cite file + source doc. TODO(...) = mechanism locked, value open.
